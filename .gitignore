.DS_Store
*.swp
*.swo
.env
.env.local
.env.*
!.env.example
node_modules/

# Sensitive business documents (not for public repo)
business-docs/

# Draft content (LinkedIn/Medium posts - not for public repo)
content/

# Backend source (deployed separately to AWS Lambda)
velonyx-website/backend/

# Terraform state and provider cache (sensitive — never commit)
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# Lambda deploy artifacts (generated locally by deploy.sh — not for the repo)
backend/chatbot-lambda/function.zip
backend/chatbot-lambda/package-lock.json
velonyx-platform/
_site/
