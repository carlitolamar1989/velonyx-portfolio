/* Velonyx Marketing Pixels Config
 * ==================================================================
 * Set META_PIXEL_ID when your Meta Pixel is live.
 *   1. Create a pixel at business.facebook.com > Events Manager > Pixels
 *   2. Copy the 15-16 digit Pixel ID
 *   3. Replace `null` below with the quoted ID (e.g. '1234567890123456')
 * The pixel only loads AFTER the visitor accepts cookies (see
 * assets/cookie-consent.js). Until META_PIXEL_ID is set, nothing fires.
 * ==================================================================
 */
window.VELONYX_MARKETING = {
  META_PIXEL_ID: null
};
